<section class="section">
  <div class="section-header d-mobile-none">
    <h1>Motoristas</h1>
  </div>

  <div class="card">
    <div class="card-body">
      <div class="table-responsive">
        <table class="table table-bordered table-md">
          <tr>
            <th>#ID</th>
            <th>Nome</th>
            <th>Telefone</th>
            <th>Messenger ID</th>
            <th>Verificado?</th>
          </tr>

          <%= render @drivers %>
        </table>
      </div>
    </div>

    <%== render partial: 'shared/pagy_nav', locals: {pagy: @pagy} %>
  </div>

</section>

<% content_for(:page_title, "Motoristas") %>
